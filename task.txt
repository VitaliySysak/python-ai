Підготовка до виконання завдання
Робота виконується на основі даних реальних банківських транзакцій, які потрібно завантажити за наступним лінком:
🔗 Bank Customer Segmentation Dataset

Ці дані можна завантажити:

Напряму у вигляді CSV-файлу

Використовуючи API для завантаження і збереження даних модуля Kaggle

Для цього потрібно:

Встановити модуль Kaggle:
bash
Copy
Edit
pip install kaggle
Завантажити вибраний набір даних на диск у дефолтну папку модуля:
python
Copy
Edit
import kagglehub
path = kagglehub.dataset_download("shivamb/bank-customer-segmentation")
print("Path to dataset files:", path)
Скопіювати завантажений файл bank_transactions.csv безпосередньо у папку майбутнього проєкту.
Обробка даних у Pandas
Використовуючи бібліотеку Pandas, необхідно:

Вибрати 1000 записів у довільному порядку (наприклад, перші 1000, останні 1000, випадкові, середні тощо).
Записати їх в інший CSV-файл (наприклад, random_1000_records.csv або last_1000_records.csv).
Для економії ресурсів у подальшій роботі використовувати цей скорочений набір даних, зберігаючи можливість використання повного набору.
Також необхідно завантажити вихідну імплементацію системи банківських рахунків із прикріпленого до завдання файлу bank_example.py.

Вимоги до виконання завдання
✅ Дотримання принципів ООП і SOLID.
✅ Використання Pandas для роботи з даними.
✅ Використання запропонованої імплементації класів у файлі bank_example.py.
✅ Не змінювати існуючі класи та методи у файлі bank_example.py, а лише розширити його функціональність.

Модифікації до bank_example.py
🔹 Створити клас-нащадок ExtendedClient на основі Client, додавши додаткові поля даних із таблиці транзакцій:

CustomerID
CustomerDOB
CustGender
CustLocation
🔹 Класи, що стосуються рахунків, залишити без змін (нумерацію рахунків реалізовувати через create_account() у Bank).

🔹 Класи, що стосуються транзакцій, залишити без змін (деталі транзакцій із таблиці можна проігнорувати).

Індивідуальне завдання (максимально 7 балів)
Розробити окремий клас, який:
✅ Створить об'єкт класу Bank і згенерує базу клієнтів, використовуючи дані з CSV-файлу з 1000 записами.
✅ Оскільки у файлі немає імен клієнтів, то ім'я формуватиметься як "CustomerID CustLocation" (поєднання цих значень).
✅ Для кожного клієнта виконати транзакцію:

Якщо клієнт чоловік → рахунок поповнюється.
Якщо клієнт жінка → з рахунку знімається сума транзакції.
✅ Вивести інформацію про 10 випадкових клієнтів та їхні рахунки, використовуючи перевизначений метод __str__.
Ускладнене завдання (додатково 3 бали, у сумі 10 балів)
Розробити окремий клас в окремому модулі, який:
✅ Отримує як вхідні параметри:

Об'єкт класу Bank (з усіма ініціалізованими клієнтами та виконаними транзакціями).
DataFrame із 1000 записами CSV-файлу.
✅ Обчислює загальну суму коштів на рахунках клієнтів (після транзакцій).
✅ Порівнює її зі сумою балансів до транзакцій (з таблиці).
✅ Виводить результати порівняння на екран.
